//Global Variables
var rightAnswers = 0;
var wrongAnswers = 0;

//function for starting the game, there has to be an intro with a "Gamestart" function

function startGame(){
	//once "Start" button is pushed the actual quiz will need to be shown
}

//each button that is selected will need to be compared to the right answer, 
//if the button selected is the right answer
rightAnswers++
//if the button selected is the wrong answer
wrongAnswers++

//Once the submit button is pressed, display the final results on screen, replacing the quiz with the results and an option to restart the quiz

function results(){
	
}

